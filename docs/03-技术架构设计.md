# 03-技术架构设计

## 技术栈选型

### 核心技术栈（第一阶段）

| 组件 | 技术选型 | 说明 |
|-----|---------|------|
| **数据库** | PostgreSQL | 成熟稳定，支持 JSON 类型，第一阶段足够 |
| **后端框架** | FastAPI（Python） | 轻量高效，适合 AI 集成 |
| **前端框架** | React | 组件化开发，生态丰富 |
| **图谱可视化** | React Flow / Cytoscape.js | 支持节点展开、关系展示、交互 |
| **AI 抽取** | GLM-5.2 API | 实体识别、关系抽取 |

### 设计原则

- 第一阶段优先简单可靠的方案
- 后续如关系查询成为瓶颈，可迁移到 Neo4j

---

## LLM API 调用设计

### GLM 接口配置

**使用智谱 GLM 的 Anthropic 协议接口**

```javascript
const llmConfig = {
  mode: "glm-coding-anthropic",
  protocol: "anthropic",
  model: "glm-5.2",
  endpoint: "https://open.bigmodel.cn/api/anthropic/v1/messages",

  // 环境变量映射
  apiKey: () => process.env.GLM_CODING_API_KEY || process.env.LLM_API_KEY,
  modelEnv: () => process.env.GLM_CODING_MODEL || process.env.LLM_MODEL,
  endpointEnv: () => process.env.GLM_CODING_ANTHROPIC_API_URL || process.env.LLM_API_URL,
};
```

### GLM 调用函数

```javascript
async function callGLM(prompt, options = {}) {
  const config = llmConfig;
  const controller = new AbortController();

  // 超时控制（默认 10 分钟）
  const timeout = setTimeout(() => controller.abort(), options.timeout || 600000);

  try {
    const payload = {
      model: config.model,
      temperature: options.temperature || 0.1,
      max_tokens: options.max_tokens || 4000,
      messages: [
        {
          role: "system",
          content: options.systemPrompt || "你是学术论文实体识别助手。"
        },
        {
          role: "user",
          content: String(prompt)
        }
      ]
    };

    // Anthropic 协议：将 system 消息单独提取
    const [systemMessage, userMessage] = payload.messages;
    payload.system = systemMessage.content;
    payload.messages = [userMessage];

    const response = await fetch(config.endpoint, {
      method: "POST",
      signal: controller.signal,
      headers: {
        "authorization": `Bearer ${config.apiKey()}`,
        "x-api-key": config.apiKey(),
        "anthropic-version": "2023-06-01",
        "content-type": "application/json"
      },
      body: JSON.stringify(payload)
    });

    if (!response.ok) {
      throw new Error(`GLM API error: ${response.status}`);
    }

    const data = await response.json();

    // Anthropic 协议响应格式
    return data.content?.[0]?.text || "";

  } finally {
    clearTimeout(timeout);
  }
}
```

### 环境变量配置

```bash
# GLM API 配置
GLM_CODING_API_KEY=your_glm_api_key
GLM_CODING_MODEL=glm-5.2
GLM_CODING_ANTHROPIC_API_URL=https://open.bigmodel.cn/api/anthropic/v1/messages

# 或者使用通用环境变量
LLM_API_KEY=your_glm_api_key
LLM_MODEL=glm-5.2
LLM_API_URL=https://open.bigmodel.cn/api/anthropic/v1/messages

# 请求控制
LLM_REQUEST_TIMEOUT_MS=600000
LLM_MAX_OUTPUT_TOKENS=12000
```

### 关键技术要点

1. **协议要求**：GLM 使用 Anthropic 协议，需要特殊的请求头（x-api-key, anthropic-version）
2. **请求格式**：system 消息需要单独提取，不是放在 messages 数组中
3. **端点格式**：必须以 `/v1/messages` 结尾
4. **超时控制**：使用 AbortController 实现超时机制
5. **成本控制**：通过 max_tokens 控制 API 调用成本

---

## 自动化流程技术决策

### 决策 1：LLM 调用策略

**选择：全量 LLM 调用**

- 每篇论文都调用 GLM 进行实体识别和关系抽取
- 成本：每篇论文约 1000-2000 tokens
- 优势：准确度最高，实现简单
- 成本控制：通过 max_tokens 和批量处理优化

### 决策 2：实体消歧策略

**基于多因素的消歧方法**：

1. **机构相似度**：同一机构同一研究方向 → 很可能是同一人
2. **论文网络**：两人有共同作者 → 可能是同一人
3. **LLM 确认**：最后调用 GLM 进行消歧确认

**消歧流程**：
```
新人名出现 → 检查是否与现有人名相似 →
  机构相同？→ 很可能是同一人 → 合并
  机构不同？→ 检查论文网络 → 有共同作者？→ 可能同一人 → LLM 确认
    → 不同人 → 创建新实体
```

### 决策 3：数据更新频率

**每天更新一次**：

- **时间**：每天凌晨自动执行
- **范围**：抓取前一天的新论文（arXiv 每天 100-200 篇 cs.AI/cs.LG 论文）
- **流程**：获取 → 抽取 → 入库 → 更新关系

### 批量处理优化

为了提高效率，采用批量处理：

```javascript
// 每次处理 10 篇论文，减少 API 调用次数
async function processPapersInBatch(papers) {
  const batchSize = 10;
  for (let i = 0; i < papers.length; i += batchSize) {
    const batch = papers.slice(i, i + batchSize);
    await callGLMForBatch(batch);
  }
}
```

### 成本估算

假设每天 150 篇新论文，每篇 1500 tokens：

- **输入**：150 篇 × 1500 tokens = 225,000 tokens/day
- **输出**：150 篇 × 2000 tokens = 300,000 tokens/day
- **月成本**：约 525,000 tokens × 30 天 × GLM 价格

具体成本需要根据 GLM-5.2 的定价计算。

---

## 第一阶段功能清单

### 核心功能（全部必须）

| 功能模块 | 说明 | 技术实现 |
|---------|------|---------|
| **论文数据获取** | 从 arXiv API 定期获取论文 | Python 脚本 + 定时任务 |
| **资讯搜索** | 简单的网络资讯搜索作为信息源头 | RSS 订阅 + 内容解析 |
| **实体识别** | GLM 抽取作者、机构、研究方向 | GLM-5.2 实体识别调用 |
| **关系抽取** | 建立4种直接关系（论文合作、项目合作、师兄弟、顾问） | 基于实体自动推导 |
| **证据链处理** | 先有证据→识别关系→提取保存证据 | 证据表存储追溯 |
| **数据存储** | PostgreSQL 存储所有实体和关系 | 数据库 Schema 设计 |
| **图谱可视化** | React + 图谱组件展示关系网络 | React Flow / Cytoscape.js |
| **搜索功能** | 按姓名、机构、研究方向搜索 | PostgreSQL 全文搜索 |
| **手动编辑** | 人工修正实体和关系 | React 编辑界面 |

### 第一阶段暂不包含

- ❌ 新闻/资讯爬取（留到第二阶段）
- ❌ 智能推荐（后续迭代）
- ❌ AI 问答（后续迭代）
- ❌ 企业关系（第二阶段）

---

## 开发时间周期

**2-4 周**是第一阶段的开发时间周期估算：

- 第 1 周：数据库设计 + 后端 API 开发
- 第 2 周：论文数据获取 + GLM 实体识别
- 第 3 周：关系抽取 + 证据链处理
- 第 4 周：前端图谱可视化 + 搜索编辑功能

具体时间根据实际开发情况调整。

---

## 版本信息

- 文档版本：V1.0
- 最后更新：2026-08-11
- 文档状态：Draft
