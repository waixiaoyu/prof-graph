# 基于本体的学术合作知识图谱系统

## 需求分析文档（Software Requirement Specification）

版本：V1.0\
文档状态：Draft

------------------------------------------------------------------------

# 1. 项目概述

## 1.1 项目名称

AI合作资源关系图谱系统

## 1.2 核心价值定位

### 核心问题

**传统痛点：人工识别合作关系，费时费力且难维护**

- 合作资源信息分散在论文、新闻、网站等多个数据源
- 人工刷新闻、看论文、扒网站 → 手动记录关系
- 信息更新频繁，依赖人工维护，成本高且易遗漏

### 系统定位

**基于公开信息的校企合作关系自动发现与维护系统**

通过 AI 自动化技术，对高校老师与企业之间的合作关系进行：
- 自动发现：从公开数据源中持续监控并发现新关系
- 自动抽取：识别实体（人员、机构、企业）及其关系
- 自动维护：关系发生变化时自动更新，无需人工干预

### 核心价值

> **积累合作方之间的潜在关系网络，为未来挖掘校企合作方提供候选参考**

帮助用户：
- 看清楚某领域内的校企合作网络
- 找到与现有合作网络有关联的目标合作方候选
- 持续跟踪关系变化，自动发现新的合作机会

### 目标用户

**主要用户**：技术合作经理、科研管理人员

**核心场景**：校企合作（产学研合作）
- 寻找某领域的专家与企业进行合作
- 了解学校老师在外部的合作情况
- 评估企业与高校的合作关系网络

### 第一阶段功能定位

**核心目标**：关系的自动发现和维护

**优先级排序**：
1. 🔥 **关系自动发现与维护**（第一阶段核心）
2. 🔥 **图谱可视化**（让人看清楚关系网络）
3. ⏳ **智能推荐**（后续迭代）
4. ⏳ **AI知识问答**（后续迭代）

**第一阶段不包含**：
- 推荐系统（找最合适的合作方）
- AI 问答（自然语言查询）
- 关系强度评分与排序

**第一阶段重点是**：
- 接入 2-3 个核心数据源
- 实现实体和关系的自动抽取
- 建立基础图谱存储和可视化
- 让用户能够看清关系网络的全貌

---

## 数据源优先级分析

### 数据源评估

| 数据源 | 优先级 | 技术难度 | 第一阶段 |
|-------|-------|---------|---------|
| 论文数据库 | 高 | 低 | ✅ 优先 |
| 高校官网 | 高 | 低 | ✅ 优先 |
| 新闻/资讯 | 高 | 极高 | ⏳ 后续 |
| 企业官网 | 中 | 中 | ⏳ 后续 |
| 专利数据库 | 中 | 中 | ⏳ 后续 |

### 第一阶段数据源策略

**优先实现**：
1. **论文数据库**（ArXiv、Google Scholar等）
   - 结构化程度高，有成熟 API
   - 可抽取：作者、机构、合作关系、研究方向
   - 自动化难度相对较低

2. **高校官网**（教师主页、院系网站）
   - 结构相对规整，爬取难度低
   - 可抽取：教师信息、研究方向、联系方式

**暂缓实现**：
- **新闻/资讯**：虽然价值高，但爬取和结构化难度极高，建议后续通过专业 API 或服务解决

---

## 第一阶段范围界定

### 分阶段策略

**第一阶段（2-4周）**：论文 + 机构关系
- 数据源：论文数据库、高校官网
- 实体：Person（教师）、Organization（高校）、Paper（论文）
- 关系：作者-论文、作者-机构、作者-作者（合作关系）

**第二阶段（4-8周）**：企业关系
- 数据源：企业官网、新闻资讯
- 实体：增加 Organization（企业）
- 关系：教师-企业（核心角色、合作经历）

### 企业关系的边界（第二阶段）

**重点关注**：
1. **教授在企业担当核心角色**
   - 创业（如：清华教授创办智谱AI）
   - 担任技术顾问、首席科学家等职务

2. **明确的合作经历**
   - 产学研项目合作
   - 联合实验室
   - 技术咨询项目

**暂不关注**：
- 一般性产学合作
- 讲座、会议等弱连接
- 间接关联关系

---

## 第一阶段数据模型设计

### 实体模型

#### Person（教师/作者）

**核心字段**：
| 字段名 | 说明 | 数据来源 |
|-------|------|---------|
| 姓名 | 中文姓名/英文姓名 | 论文作者、教师主页 |
| 机构 | 所属高校/院系 | 论文署名单位、主页 |
| 研究方向 | 技术领域标签（多标签） | 论文关键词、主页声明 |

**扩展字段**：
| 字段名 | 说明 | 数据来源 |
|-------|------|---------|
| 职位/职称 | 教授/副教授/讲师 | 教师主页 |
| 联系方式 | 邮箱/电话 | 教师主页 |
| 个人简介 | 研究背景描述 | 教师主页 |
| 个人主页 | 教师/学者主页 URL | 教师/学者主页 |

**研究方向表示**：
- 使用多标签字段，如：["深度学习", "计算机视觉", "NLP"]
- 支持多个研究方向的同时表达

#### Organization（机构）

| 字段名 | 说明 | 第一阶段 |
|-------|------|---------|
| 名称 | 高校/院系/实验室名称 | ✅ 必须 |
| 层级 | 校级/院级/实验室 | ✅ 必须 |
| 官网 | 机构主页 URL | □ 可选 |
| 地点 | 国家/城市 | □ 可选 |

**层级说明**：
- 校级：如"清华大学"
- 院级：如"清华大学计算机系"
- 实验室级：如"清华大学知识工程实验室"

#### Paper（论文）

| 字段名 | 说明 | 第一阶段 |
|-------|------|---------|
| 标题 | 论文标题 | ✅ 必须 |
| 作者 | 作者列表（关联 Person） | ✅ 必须 |
| 机构 | 署名机构（关联 Organization） | ✅ 必须 |
| 发布时间 | 发表年份 | ✅ 必须 |
| 来源 | 会议/期刊（如 NeurIPS/ICML） | □ 可选 |
| 摘要 | 论文摘要 | □ 可选 |
| 关键词 | 研究方向标签 | ⭐ 重要 |

---

### 核心设计理念

> **系统核心主键：人（合作方）**

**设计原则**：
- Person（合作方）是中心实体
- 其他实体（Organization、Paper）围绕合作关系展开
- 关系网络以"人"为中心进行构建和展示

**数据视角**：
- 不是"论文有哪些作者"
- 而是"这个人发表了哪些论文、与哪些人有合作关系"

这个设计理念将影响：
- 数据库 schema 设计（以 Person 为核心的关系表）
- 图谱可视化的节点展开逻辑（从人开始展开关系网络）
- 查询 API 的设计（以人为主要查询维度）

---

## 关系模型设计（知识图谱视角）

### 核心设计理念

> **这是一张知识图谱，而不仅仅是关系数据库**

**传统关系数据库 vs 知识图谱**：

| 传统关系数据库 | 知识图谱 |
|---------------|---------|
| 表内关系（外键） | 关系本身是实体 |
| Person → Paper | Person ←[合作关系]→ Person |
| 关系是结构化的 | 关系有**证据、属性、语义** |
| 表关系的"原因"不存储 | 关系的"为什么"是核心 |

### 关系分类

**表内关系（传统外键关系）**：
- Person ↔ Paper（作者-发表-论文）
- Person ↔ Organization（作者-任职-机构）
- Paper ↔ Organization（论文-署名-机构）

**表外关系（知识图谱核心）**：
- Person ↔ Person（合作关系）⭐
  - 关系属性：合作论文数量、最近合作时间、合作领域
  - 关系证据：具体哪些论文证明了这种合作关系
  - 关系强度：基于合作频次、时间、领域相似度

### 关系的"证据链"设计

**知识图谱的核心是回答"为什么"**：
- 为什么 A 和 B 是合作关系？ → 因为他们共同发表了论文 X、Y、Z
- 为什么 A 和 C 是潜在合作关系？ → 因为他们都与 B 有合作关系，且研究方向相似

这意味着：
1. 关系本身需要存储**证据列表**（如：[Paper1, Paper2, ...]）
2. 关系需要**动态计算**属性（如：合作强度）
3. 关系可以有**多种类型**（如：强合作、弱合作、潜在合作）

---

## 第一阶段关系类型定义

### 关系类型（第一阶段）

| 关系类型 | 说明 | 证据来源 | 示例 |
|---------|------|---------|------|
| **论文合作** | 共同发表过论文 | 共同作者的论文 | A 和 B 共同发表了 3 篇论文 |
| **同机构关系** | 在同一机构工作 | 论文署名单位相同 | A 和 B 都署名"清华大学计算机系" |

**暂不包含**：
- 引用关系（太宽泛，任何人都可以引用）

### 关系属性设计

**每个关系必须包含**：

| 属性 | 说明 | 示例 |
|-----|------|-----|
| **关系类型** | 论文合作/同机构关系 | "论文合作" |
| **证据列表** | 支撑该关系的证据对象 | [Paper1, Paper2, ...] 或 [Organization1] |
| **证据描述** | 人类可读的证据说明 | "基于论文：《Attention Is All You Need》等 3 篇" |
| **时间信息** | 关系的时间范围 | "2018-2024" 或 "最近合作：2024年" |
| **置信度** | AI 抽取的可靠性 | 0.95 |

### 证据链设计原则

> **每个关系都必须有明确的证据链，确保可信度**

**证据链示例**：

```
关系：张教授 —— 李教授
类型：论文合作
证据：
  - 论文：《Attention Is All You Need》（2018，NeurIPS）
  - 论文：《BERT: Pre-training of Deep Bidirectional Transformers》（2019，NAACL）
  - 论文：《GPT-3: Language Models are Few-Shot Learners》（2020，NeurIPS）
证据描述：基于 3 篇合作论文，最近合作于 2020 年
置信度：1.0（基于明确的论文数据）
```

**同机构关系示例**：

```
关系：王教授 —— 赵教授
类型：同机构关系
证据：
  - 机构：清华大学计算机系（从两人多论文署名单位推断）
证据描述：两人在多篇论文中均署名"清华大学计算机系"
置信度：0.8（基于推断）
```

### 设计原则

1. **所有关系必须有证据**：无证据的关系不存储
2. **证据必须是可追溯的**：证据对象本身存在于数据库中
3. **关系类型明确区分**：不同类型的关系有不同的语义
4. **置信度标注**：AI 抽取的关系必须标注可信度

---

## 技术架构设计

### 技术栈选型（第一阶段）

| 组件 | 技术选型 | 说明 |
|-----|---------|------|
| **数据库** | PostgreSQL | 成熟稳定，支持 JSON 类型，第一阶段足够 |
| **后端框架** | FastAPI（Python） | 轻量高效，适合 AI 集成 |
| **前端框架** | React | 组件化开发，生态丰富 |
| **图谱可视化** | React Flow / Cytoscape.js | 支持节点展开、关系展示、交互 |
| **AI 抽取** | LLM API（GPT/Claude） | 实体识别、关系抽取 |

**设计原则**：
- 第一阶段优先简单可靠的方案
- 后续如关系查询成为瓶颈，可迁移到 Neo4j

---

## 数据库 Schema 设计

### 核心表结构

#### 1. persons（人员表）

```sql
CREATE TABLE persons (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    organization_id INTEGER REFERENCES organizations(id),
    research_tags TEXT[], -- 多标签研究方向
    position VARCHAR(50), -- 职位/职称
    contact_info TEXT, -- 联系方式
    bio TEXT, -- 个人简介
    homepage_url VARCHAR(500), -- 个人主页
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);
```

#### 2. organizations（机构表）

```sql
CREATE TABLE organizations (
    id SERIAL PRIMARY KEY,
    name VARCHAR(200) NOT NULL,
    level VARCHAR(20), -- 'university'/'department'/'lab'
    website_url VARCHAR(500),
    location VARCHAR(100),
    created_at TIMESTAMP DEFAULT NOW()
);
```

#### 3. papers（论文表）

```sql
CREATE TABLE papers (
    id SERIAL PRIMARY KEY,
    title TEXT NOT NULL,
    publish_year INTEGER,
    source VARCHAR(200), -- 会议/期刊名称
    abstract TEXT,
    keywords TEXT[],
    doi VARCHAR(100),
    created_at TIMESTAMP DEFAULT NOW()
);
```

#### 4. relationships（关系表）

```sql
CREATE TABLE relationships (
    id SERIAL PRIMARY KEY,
    person1_id INTEGER REFERENCES persons(id),
    person2_id INTEGER REFERENCES persons(id),
    relationship_type VARCHAR(50) NOT NULL, -- 'paper_cooperation'/'same_organization'
    confidence FLOAT DEFAULT 1.0,
    time_range VARCHAR(50), -- 如 "2018-2024"
    latest_evidence_time TIMESTAMP,
    evidence_description TEXT,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW(),
    UNIQUE(person1_id, person2_id, relationship_type)
);
```

#### 5. evidence（证据表）

```sql
CREATE TABLE evidence (
    id SERIAL PRIMARY KEY,
    relationship_id INTEGER REFERENCES relationships(id),
    evidence_type VARCHAR(50) NOT NULL, -- 'paper'/'organization'
    evidence_id INTEGER NOT NULL, -- paper_id 或 organization_id
    evidence_description TEXT,
    created_at TIMESTAMP DEFAULT NOW()
);
```

#### 6. person_paper_affiliation（作者-论文-机构关联表）

```sql
CREATE TABLE person_paper_affiliation (
    id SERIAL PRIMARY KEY,
    person_id INTEGER REFERENCES persons(id),
    paper_id INTEGER REFERENCES papers(id),
    organization_id INTEGER REFERENCES organizations(id),
    author_order INTEGER, -- 作者顺序
    created_at TIMESTAMP DEFAULT NOW(),
    UNIQUE(person_id, paper_id, organization_id)
);
```

### 设计说明

1. **证据单独存储**：证据表记录每个关系的具体证据，支持追溯和查询
2. **关系唯一性**：通过 UNIQUE 约束确保相同两人、相同类型的关系只存一条
3. **时间信息**：关系表记录时间范围和最新证据时间
4. **灵活的标签存储**：使用 TEXT[] 存储多标签（研究方向、关键词）

---

## LLM API 调用设计

### 参考实现：paper-insight 项目（GLM 接口）

参考项目 [waixiaoyu/paper-insight](https://github.com/waixiaoyu/paper-insight) 的实现，使用 **智谱 GLM 的 Anthropic 协议接口**。

### GLM 配置（Anthropic 协议）

```javascript
const llmConfig = {
  mode: "glm-coding-anthropic",
  protocol: "anthropic",
  model: "glm-5.2",
  endpoint: "https://open.bigmodel.cn/api/anthropic/v1/messages",
  
  // 环境变量映射
  apiKey: () => process.env.GLM_CODING_API_KEY || process.env.LLM_API_KEY,
  modelEnv: () => process.env.GLM_CODING_MODEL || process.env.LLM_MODEL,
  endpointEnv: () => process.env.GLM_CODING_ANTHROPIC_API_URL || process.env.LLM_API_URL,
};
```

### GLM 调用函数

```javascript
async function callGLM(prompt, options = {}) {
  const config = llmConfig;
  const controller = new AbortController();
  
  // 超时控制（默认 10 分钟）
  const timeout = setTimeout(() => controller.abort(), options.timeout || 600000);
  
  try {
    const payload = {
      model: config.model,
      temperature: options.temperature || 0.1,
      max_tokens: options.max_tokens || 4000,
      messages: [
        {
          role: "system",
          content: options.systemPrompt || "你是学术论文实体识别助手。"
        },
        {
          role: "user",
          content: String(prompt)
        }
      ]
    };
    
    // Anthropic 协议：将 system 消息单独提取
    const [systemMessage, userMessage] = payload.messages;
    payload.system = systemMessage.content;
    payload.messages = [userMessage];
    
    const response = await fetch(config.endpoint, {
      method: "POST",
      signal: controller.signal,
      headers: {
        "authorization": `Bearer ${config.apiKey()}`,
        "x-api-key": config.apiKey(),
        "anthropic-version": "2023-06-01",
        "content-type": "application/json"
      },
      body: JSON.stringify(payload)
    });
    
    if (!response.ok) {
      throw new Error(`GLM API error: ${response.status}`);
    }
    
    const data = await response.json();
    
    // Anthropic 协议响应格式
    return data.content?.[0]?.text || "";
    
  } finally {
    clearTimeout(timeout);
  }
}
```

### 环境变量配置

```bash
# GLM API 配置
GLM_CODING_API_KEY=your_glm_api_key
GLM_CODING_MODEL=glm-5.2
GLM_CODING_ANTHROPIC_API_URL=https://open.bigmodel.cn/api/anthropic/v1/messages

# 或者使用通用环境变量
LLM_API_KEY=your_glm_api_key
LLM_MODEL=glm-5.2
LLM_API_URL=https://open.bigmodel.cn/api/anthropic/v1/messages

# 请求控制
LLM_REQUEST_TIMEOUT_MS=600000
LLM_MAX_OUTPUT_TOKENS=12000
```

### GLM 响应格式处理

```javascript
// Anthropic 协议响应格式
{
  "id": "msg_xxx",
  "type": "message",
  "role": "assistant",
  "content": [
    {
      "type": "text",
      "text": "{...JSON response...}"
    }
  ],
  "model": "glm-5.2",
  "stop_reason": "end_turn"
}

// 提取文本内容
const extractText = (data) => {
  return data.content?.[0]?.text || "";
};
```

### 关键技术要点

1. **协议要求**：GLM 使用 Anthropic 协议，需要特殊的请求头（x-api-key, anthropic-version）
2. **请求格式**：system 消息需要单独提取，不是放在 messages 数组中
3. **端点格式**：必须以 `/v1/messages` 结尾
4. **超时控制**：使用 AbortController 实现超时机制
5. **成本控制**：通过 max_tokens 控制 API 调用成本

---

## 第一阶段 MVP 功能清单

### 核心功能（全部必须）

| 功能模块 | 说明 | 技术实现 |
|---------|------|---------|
| **论文数据获取** | 从 arXiv API 定期获取论文 | Python 脚本 + 定时任务 |
| **资讯搜索** | 简单的网络资讯搜索作为信息源头 | 搜索引擎 API + 内容解析 |
| **实体识别** | GLM 抽取作者、机构、研究方向 | GLM-5.2 实体识别调用 |
| **关系抽取** | 建立论文合作、同机构关系 | 基于实体自动推导 |
| **证据链处理** | 先有证据→识别关系→提取保存证据 | 证据表存储追溯 |
| **数据存储** | PostgreSQL 存储所有实体和关系 | 数据库 Schema 设计 |
| **图谱可视化** | React + 图谱组件展示关系网络 | React Flow / Cytoscape.js |
| **搜索功能** | 按姓名、机构、研究方向搜索 | PostgreSQL 全文搜索 |
| **手动编辑** | 人工修正实体和关系 | React 编辑界面 |

### 第一阶段暂不包含

- ❌ 高校官网爬取（留到第二阶段）
- ❌ 智能推荐（后续迭代）
- ❌ AI 问答（后续迭代）
- ❌ 企业关系（第二阶段）

### 开发时间周期

**2-4 周**是第一阶段的开发时间周期估算：
- 第 1 周：数据库设计 + 后端 API 开发
- 第 2 周：论文数据获取 + GLM 实体识别
- 第 3 周：关系抽取 + 证据链处理
- 第 4 周：前端图谱可视化 + 搜索编辑功能

具体时间根据实际开发情况调整。

---

## 证据链处理逻辑更正

### 正确的流程

> **先有证据 → 识别关系 → 提取保存证据**

### 流程说明

```
原始数据（论文、资讯）
  ↓
【证据提取】保存原始证据对象
  ↓
【实体识别】GLM 识别人员、机构
  ↓
【关系识别】基于证据识别关系类型
  ↓
【关系存储】建立关系 + 关联证据
```

### 数据库逻辑

1. **先存证据**：论文、资讯作为独立实体存入数据库
2. **再识关系**：基于证据实体识别人员之间的关系
3. **关联证据**：关系表关联证据表，支持追溯

### 示例流程

```
1. 获取论文《Attention Is All You Need》
   → 存入 papers 表（id=1）

2. GLM 识别实体
   → 识别出作者：Ashish Vaswani, ...
   → 识别出机构：Google Brain

3. 建立关系
   → 发现 Ashish Vaswani 与其他作者有合作关系
   → 存入 relationships 表

4. 关联证据
   → 存入 evidence 表，关联 paper_id=1
   → 支持追溯："为什么他们是合作关系？因为共同发表了这篇论文"
```

---

## 网络资讯搜索功能

### 功能定位

**简单的网络资讯搜索作为第一阶段信息源头**

### 技术实现

| 方案 | 说明 | 难度 |
|-----|------|------|
| **搜索引擎 API** | 使用 Google/Bing API 搜索相关资讯 | 中 |
| **RSS 订阅** | 订阅科技媒体 RSS 获取资讯 | 低 |
| **关键词监控** | 监控特定关键词的资讯更新 | 中 |

### 推荐方案

**RSS 订阅 + 简单内容解析**
- 订阅主要科技媒体的 RSS（如 36氪、机器之心等）
- 定期获取新资讯
- GLM 解析资讯中的实体和关系
- 建立资讯作为证据的关联关系

---

## 第一阶段资讯源技术分级

### 第一优先级（低难度）：RSS 订阅类

| 资讯源 | 类型 | 技术难度 | 实现周期 |
|-------|------|---------|---------|
| **arXiv News** | 学术资讯 | ⭐ 低 | 1-2 天 |
| **机器之心 RSS** | AI 专项 | ⭐ 低 | 1-2 天 |
| **新智元 RSS** | AI 专项 | ⭐ 低 | 1-2 天 |
| **量子位 RSS** | AI 专项 | ⭐ 低 | 1-2 天 |

**技术实现**：直接解析 RSS XML，获取标题、摘要、链接

---

### 第二优先级（中难度）：高校官网

| 资讯源 | 类型 | 技术难度 | 实现周期 |
|-------|------|---------|---------|
| **清华官网新闻** | 高校动态 | ⭐⭐ 中 | 3-5 天 |
| **北大官网新闻** | 高校动态 | ⭐⭐ 中 | 3-5 天 |
| **其他高校官网** | 高校动态 | ⭐⭐ 中 | 每个学校 2-3 天 |

**技术实现**：针对性爬虫，解析新闻列表页和详情页

---

### 第三优先级（高难度）：综合科技媒体（后续迭代）

| 资讯源 | 类型 | 技术难度 | 实现周期 |
|-------|------|---------|---------|
| **36氪** | 综合科技 | ⭐⭐⭐ 高 | 1-2 周 |
| **虎嗅** | 综合科技 | ⭐⭐⭐ 高 | 1-2 周 |
| **钛媒体** | 综合科技 | ⭐⭐⭐ 高 | 1-2 周 |

**技术实现**：需要处理反爬、登录、动态加载等复杂情况

---

## 第一阶段实施策略

### Week 1-2：RSS 订阅类 + 论文数据获取
- arXiv RSS + 机器之心 RSS
- 基础的 GLM 实体识别
- 论文数据获取和处理

### Week 3-4：高校官网爬取 + 关系抽取
- 清华官网新闻 + 北大官网新闻
- 完善关系抽取和证据链处理
- 前端图谱可视化

### 后续迭代：综合科技媒体
- 等基础稳定后再扩展

---

## 第一阶段前端界面设计

### 核心页面结构

| 页面 | 功能 | 优先级 |
|-----|------|-------|
| **图谱主页** | 关系网络可视化、节点展开、搜索 | P0 |
| **搜索页** | 按姓名、机构、研究方向搜索 | P0 |
| **管理后台** | 手动修正实体和关系 | P0 |
| **数据监控** | 显示数据更新状态、最近新增实体 | P1 |

### 图谱主页设计

**核心功能**：
1. **大规模节点支持**：支持 1000+ 节点展示
2. **过滤能力**：按关系类型、机构、研究方向过滤
3. **节点展开**：点击节点显示其合作关系
4. **搜索定位**：搜索后高亮显示相关节点
5. **缩放拖拽**：自由导航图谱

**交互示例**：
```
搜索"张教授" → 高亮显示张教授节点
  → 点击张教授节点 → 展开其合作关系
  → 显示合作者列表 + 合作论文证据
```

**过滤选项**：
- 按机构过滤：只显示某高校的人员网络
- 按关系类型过滤：只显示论文合作关系或同机构关系
- 按研究方向过滤：只显示某领域的人员

### 搜索功能设计

**双模式展示**：
- **图谱模式**：搜索结果在图谱中高亮显示
- **列表模式**：搜索结果以列表形式展示详细信息

**搜索维度**：
- 按姓名搜索（支持模糊匹配）
- 按机构搜索
- 按研究方向搜索
- 组合搜索

### 管理后台设计

**定位**：管理员专用功能，不需要特别美观

**核心功能**：
1. **修正姓名/机构名**：修正实体识别错误
2. **添加/删除关系**：手动调整关系
3. **修改研究方向**：更新研究方向标签
4. **合并重复实体**：处理实体消歧问题

**实现方式**：
- 单独的编辑页面
- 表单化操作界面
- 操作日志记录

---

## 关系类型与置信度评估设计

### 核心理念

> **关系结果：都是直接关系（A ⇄ B）**  
> **挖掘手段：用间接手段或推导方法发现直接关系**  
> **置信度评估：每种关系都有专门的置信度计算方式**

### 第一阶段挖掘的4种直接关系

| 关系类型 | 说明 | 挖掘手段 | 价值 |
|---------|------|---------|------|
| **论文合作关系** | 共同发表过论文 | 从论文数据库直接获取 | ⭐⭐⭐ 核心 |
| **项目合作关系** | 共同参与过项目 | 从多个资讯源推断 | ⭐⭐⭐ 核心 |
| **师兄弟关系** | 同一实验室的同学、师生关系 | 从多种数据源推导 | ⭐⭐⭐ 核心 |
| **顾问关系** | 担任某机构的技术顾问、学术顾问 | 从资讯中提取 | ⭐⭐ 重要 |

---

## 1. 论文合作关系的置信度评估

### 置信度评估公式

```
置信度 = 0.4 × 姓名确信度 + 0.6 × 机构确信度
```

### 各维度确信度定义

**姓名确信度**：
| 情况 | 确信度 |
|-----|-------|
| 姓名完全匹配 | 1.0 |
| 姓名相似（如"Zhang Wei" vs "Wei Zhang"） | 0.7 |
| 姓名模糊匹配（如"李明" vs "Ming Li"） | 0.5 |
| 姓名完全不匹配但有其他证据 | 0.3 |

**机构确信度**：
| 情况 | 确信度 |
|-----|-------|
| 机构完全匹配 | 1.0 |
| 机构相似（中英文对照） | 0.8 |
| 机构模糊匹配（不同层级表述） | 0.6 |
| 机构不明确或缺失 | 0.4 |

### 置信度分级

| 置信度范围 | 分类 | 处理方式 |
|-----------|------|---------|
| 0.8-1.0 | 高置信度 | 正常显示 |
| 0.5-0.7 | 中置信度 | 标注"待确认" |
| 0.3-0.4 | 低置信度 | 标注"低可信"或默认隐藏 |
| < 0.3 | 极低置信度 | 默认隐藏，需要人工确认 |

---

## 2. 项目合作关系的置信度评估

### 核心评估维度

| 维度 | 说明 | 权重 |
|-----|------|------|
| **信息明确性** | 表达是否清晰明确 | 30% |
| **信息充分性** | 信息是否足够充分 | 30% |
| **数据源可信度** | 数据源本身的可靠性 | 20% |
| **可访问性** | 项目组成员定义的可获取性 | 20% |

### 置信度评估公式

```
置信度 = 0.3 × 信息明确性 + 0.3 × 信息充分性 + 0.2 × 数据源可信度 + 0.2 × 可访问性
```

### 各维度确信度定义

**信息明确性确信度**：
| 情况 | 确信度 | 示例 |
|-----|-------|------|
| 明确列出项目成员 | 1.0 | "项目成员：张教授、李教授、王工程师" |
| 明确说明参与关系 | 0.8 | "张教授和李教授共同参与了XX项目" |
| 暗示参与关系 | 0.5 | "张教授所在团队参与了XX项目" |
| 关系模糊 | 0.3 | "张教授与XX项目有关联" |

**信息充分性确信度**：
| 情况 | 确信度 | 示例 |
|-----|-------|------|
| 详细说明角色和时间 | 1.0 | "张教授担任项目负责人，2022-2024年" |
| 说明参与角色 | 0.8 | "张教授是该项目的核心成员" |
| 仅提及参与 | 0.5 | "张教授参与了该项目" |
| 信息较少 | 0.3 | "张教授与该项目相关" |

**数据源可信度确信度**：
| 情况 | 确信度 |
|-----|-------|
| 官方公告 | 1.0 |
| 知名媒体 | 0.8 |
| 一般资讯 | 0.6 |

**可访问性确信度**：
| 情况 | 确信度 | 示例 |
|-----|-------|------|
| 直接列出成员 | 1.0 | "项目成员：张教授、李教授" |
| 可访问到完整定义 | 0.8 | "项目组成员：见XX附件" |
| 可访问到部分定义 | 0.6 | "项目组由XX学院专家组成" |
| 定义难以获取 | 0.4 | "项目组成员由相关部门确定" |
| 定义不明确 | 0.2 | "项目组相关专家" |

---

## 3. 师兄弟关系的置信度评估

### 主要数据来源类型

| 数据源类型 | 示例 | 推断方式 |
|-----------|------|---------|
| **导师主页学生列表** | "当前学生：张三、李四、王五" | 同一导师的学生 → 同学关系 |
| **实验室成员列表** | "实验室成员：教授A、博士生B、C" | 同一实验室 → 师生/同学关系 |
| **毕业生名单** | "2020届博士毕业生：张三、李四" | 同届毕业生 → 同学关系 |
| **论文致谢** | "感谢导师XX教授和实验室同学们" | 暗示同实验室关系 |
| **直接声明** | "师从XX教授" | 直接师生关系 |

### 置信度评估公式

```
置信度 = 0.4 × 数据源可信度 + 0.3 × 关系推断确定性 + 0.2 × 信息明确性 + 0.1 × 时间吻合度
```

### 各维度确信度定义

**数据源可信度确信度**：
| 情况 | 确信度 |
|-----|-------|
| 官方列表（导师主页、实验室网站） | 1.0 |
| 半官方列表（院系网站） | 0.8 |
| 毕业生名单 | 0.8 |
| 论文致谢 | 0.6 |
| 推断信息 | 0.4 |

**关系推断确定性确信度**：
| 情况 | 确信度 | 示例 |
|-----|-------|------|
| 明确在同一列表中 | 1.0 | 两人都在"当前学生"列表中 |
| 明确在同一类别中 | 0.9 | 两人都在"博士生"类别中 |
| 暗示同实验室 | 0.7 | 致谢中提到"实验室同学" |
| 推断可能有关联 | 0.5 | 同一导师的不同时期学生 |

**信息明确性确信度**：
| 情况 | 确信度 | 示例 |
|-----|-------|------|
| 明确分类和身份 | 1.0 | "博士生：张三、李四" |
| 明确身份 | 0.8 | "实验室成员：张三、李四" |
| 仅列出名字 | 0.6 | "团队成员：张三、李四、王五" |
| 名字模糊 | 0.4 | "相关研究人员" |

**时间吻合度确信度**：
| 情况 | 确信度 |
|-----|-------|
| 时间明确且吻合 | 1.0 |
| 时间接近 | 0.7 |
| 时间不明确 | 0.5 |
| 时间不吻合 | 0.2 |

---

## 4. 顾问关系的置信度评估

### 置信度评估公式

```
置信度 = 0.4 × 信息明确性 + 0.3 × 数据源可信度 + 0.2 × 信息充分性 + 0.1 × 时效性
```

### 各维度确信度定义

**信息明确性确信度**：
| 情况 | 确信度 | 示例 |
|-----|-------|------|
| 明确说明顾问角色 | 1.0 | "担任XX公司首席技术顾问" |
| 明确说明专家角色 | 0.8 | "担任XX公司技术专家委员会委员" |
| 暗示顾问关系 | 0.5 | "为XX公司提供技术咨询" |
| 关系模糊 | 0.3 | "与XX公司有合作关系" |

**数据源可信度确信度**：
| 情况 | 确信度 |
|-----|-------|
| 官方公告（企业/高校官网） | 1.0 |
| 知名媒体 | 0.8 |
| 一般资讯 | 0.6 |
| 社交媒体 | 0.4 |

**信息充分性确信度**：
| 情况 | 确信度 | 示例 |
|-----|-------|------|
| 详细说明角色和期限 | 1.0 | "2022-2025年担任首席技术顾问" |
| 说明角色 | 0.8 | "担任技术顾问" |
| 仅提及关系 | 0.5 | "与XX公司有顾问关系" |
| 信息较少 | 0.3 | "为XX公司提供咨询" |

**时效性确信度**：
| 情况 | 确信度 | 示例 |
|-----|-------|------|
| 明确当前有效 | 1.0 | "现任首席技术顾问" |
| 明确近期关系 | 0.8 | "2023年担任技术顾问" |
| 关系时间不明确 | 0.5 | "曾担任技术顾问" |
| 历史关系 | 0.3 | "2015年曾任技术顾问" |

---

## 置信度评估总结

| 关系类型 | 核心评估维度 | 权重分配 |
|---------|-------------|---------|
| **论文合作** | 姓名确信度、机构确信度 | 0.4姓名 + 0.6机构 |
| **项目合作** | 信息明确性、信息充分性、数据源可信度、可访问性 | 0.3明确 + 0.3充分 + 0.2可信 + 0.2可访问 |
| **师兄弟关系** | 数据源可信度、关系推断确定性、信息明确性、时间吻合度 | 0.4数据源 + 0.3推断 + 0.2明确 + 0.1时间 |
| **顾问关系** | 信息明确性、数据源可信度、信息充分性、时效性 | 0.4明确 + 0.3数据源 + 0.2充分 + 0.1时效 |

### 置信度分级（统一标准）

| 置信度范围 | 分类 | 处理方式 |
|-----------|------|---------|
| 0.8-1.0 | 高置信度 | 正常显示 |
| 0.5-0.7 | 中置信度 | 标注"待确认" |
| 0.3-0.4 | 低置信度 | 标注"低可信"或默认隐藏 |
| < 0.3 | 极低置信度 | 默认隐藏，需要人工确认 |

---

## 第一阶段部署与运行方案

### 部署方式：本地部署

**选择理由**：
- 数据安全，所有数据在本地
- 部署简单，不需要复杂的服务器配置
- 单机使用，无需考虑多用户并发

### 系统架构

```
本地机器
├── PostgreSQL 数据库（本地安装或 Docker）
├── Python 后端服务（FastAPI）
├── React 前端（静态文件）
└── 定时任务脚本（数据更新）
```

### 数据更新机制

**两种触发方式**：

1. **自动更新**：每天凌晨自动执行
   - 使用系统定时任务（如 cron 或 Windows 任务计划）
   - 自动抓取论文和资讯
   - 自动抽取关系并入库

2. **手动触发**：用户主动启动更新
   - 在管理后台提供"立即更新"按钮
   - 实时显示更新进度
   - 更新完成后刷新数据

### 数据管理与安全

**简化方案**：
- ❌ 不需要用户权限控制系统（单机使用）
- ❌ 不需要复杂的数据备份机制（可选手动备份）
- ✅ 基本的数据导出功能（便于备份和迁移）

### 运行环境要求

**最低配置**：
- CPU：4 核心以上
- 内存：8GB 以上
- 存储：50GB 以上（数据库 + 论文缓存）
- 网络：稳定的网络连接（调用 GLM API）

**软件依赖**：
- Python 3.10+
- Node.js 18+（前端构建）
- PostgreSQL 14+
- 浏览器：Chrome/Edge/Firefox 最新版

---

## 自动化流程技术决策

### 决策 1：LLM 调用策略

**选择：全量 LLM 调用**

- 每篇论文都调用 GLM 进行实体识别和关系抽取
- 成本：每篇论文约 1000-2000 tokens
- 优势：准确度最高，实现简单
- 成本控制：通过 max_tokens 和批量处理优化

### 决策 2：实体消歧策略

**基于多因素的消歧方法**：

1. **机构相似度**：同一机构同一研究方向 → 很可能是同一人
2. **论文网络**：两人有共同作者 → 可能是同一人
3. **LLM 确认**：最后调用 GLM 进行消歧确认

**消歧流程**：
```
新人名出现 → 检查是否与现有人名相似 → 
  机构相同？→ 很可能是同一人 → 合并
  机构不同？→ 检查论文网络 → 有共同作者？→ 可能同一人 → LLM 确认
    → 不同人 → 创建新实体
```

### 决策 3：数据更新频率

**每天更新一次**：

- **时间**：每天凌晨自动执行
- **范围**：抓取前一天的新论文（arXiv 每天 100-200 篇 cs.AI/cs.LG 论文）
- **流程**：获取 → 抽取 → 入库 → 更新关系

### 批量处理优化

为了提高效率，采用批量处理：

```javascript
// 每次处理 10 篇论文，减少 API 调用次数
async function processPapersInBatch(papers) {
  const batchSize = 10;
  for (let i = 0; i < papers.length; i += batchSize) {
    const batch = papers.slice(i, i + batchSize);
    await callGLMForBatch(batch);
  }
}
```

### 成本估算

假设每天 150 篇新论文，每篇 1500 tokens：

- **输入**：150 篇 × 1500 tokens = 225,000 tokens/day
- **输出**：150 篇 × 2000 tokens = 300,000 tokens/day
- **月成本**：约 525,000 tokens × 30 天 × GLM 价格

具体成本需要根据 GLM-5.2 的定价计算。

### 技术难点

- **论文数据源**：实体消歧（同一个人在不同论文中的名字可能不同）
- **高校官网**：网站结构不统一，需要针对性的解析规则

## 1.3 项目定位

本系统旨在建设一个面向技术合作管理场景的智能化资源关系管理平台。

系统通过知识图谱、自然语言处理、大模型分析等技术，对合作资源进行结构化管理，并自动发现、维护和扩展资源之间的关联关系。

系统最终形成： - 人员资源网络 - 机构合作网络 - 技术领域关联网络 -
项目合作网络

帮助用户实现合作资源的数字化管理和智能分析。

------------------------------------------------------------------------

# 2. 建设背景

## 2.1 当前问题

### 资源信息分散

合作专家、企业、高校等信息分散在
Excel、文档、邮件、会议纪要等多个数据源中，缺少统一管理。

### 资源关系不可见

传统资源库只能记录实体信息，无法表达： - 人与机构关系 -
人与技术方向关系 - 人与项目关系 - 机构之间合作关系

### 资源维护成本高

资源变化依赖人工更新，无法自动吸收： - 新论文 - 新合作 - 新技术动态

### 缺少智能分析能力

无法快速回答： - 某技术方向有哪些核心资源？ - 某专家有哪些合作关系？ -
哪些资源值得重点维护？

------------------------------------------------------------------------

# 3. 建设目标

## 3.1 总体目标

建设一个能够自动采集、理解、关联和展示合作资源关系的智能知识图谱系统。

## 3.2 具体目标

### 资源结构化

将非结构化资源转换为标准化数据： - 专家 - 企业 - 高校 - 技术 - 论文 -
项目

### 关系网络化

建立实体之间关系模型：

专家 ------ 任职 ------ 机构

专家 ------ 研究 ------ 技术方向

专家 ------ 发表 ------ 论文

机构 ------ 合作 ------ 机构

### 关系自动发现

通过 AI 自动发现： - 新实体 - 新关系 - 潜在合作连接

### 智能查询分析

支持自然语言查询资源网络。

------------------------------------------------------------------------

# 4. 用户角色

## 技术合作经理

权限： - 查看资源地图 - 管理合作资源 - 查询关系网络 - 查看 AI 分析结果

## 管理人员

权限： - 查看整体资源情况 - 查看合作网络分析 - 查看资源价值排名

## 系统管理员

权限： - 数据管理 - 用户管理 - 系统配置

------------------------------------------------------------------------

# 5. 功能需求

## 5.1 资源实体管理

系统支持以下实体：

### Person（人员）

字段： - 姓名 - 所属机构 - 职位 - 研究方向 - 联系方式 - 个人简介

### Organization（机构）

字段： - 名称 - 类型 - 行业 - 国家地区 - 简介

### Technology（技术）

字段： - 技术名称 - 分类 - 描述 - 热度

### Paper（论文）

字段： - 标题 - 作者 - 机构 - 发布时间 - 研究方向

### Project（项目）

字段： - 项目名称 - 参与方 - 时间 - 状态

------------------------------------------------------------------------

# 5.2 关系管理

支持关系：

  关系   说明
  ------ ------------
  任职   人员与机构
  研究   人员与技术
  发表   人员与论文
  合作   机构之间
  参与   人员与项目
  引用   论文之间

关系属性： - 类型 - 置信度 - 数据来源 - 创建时间 - 更新时间

------------------------------------------------------------------------

# 5.3 图谱可视化

支持： - 节点展示 - 节点展开 - 搜索 - 缩放 - 拖拽 - 关系过滤

示例：

AI Agent\
↓\
张教授\
↓\
清华大学 / Paper / 企业合作

------------------------------------------------------------------------

# 5.4 AI 自动关系抽取

输入： - PDF论文 - 新闻网页 - Word文档 - PPT - 会议纪要

流程：

原始数据\
↓\
文本解析\
↓\
实体识别\
↓\
关系抽取\
↓\
人工确认\
↓\
写入知识图谱

------------------------------------------------------------------------

# 5.5 AI资源画像

自动生成： - 专家背景 - 技术方向 - 论文情况 - 关联机构 - 合作建议

------------------------------------------------------------------------

# 5.6 AI知识问答

支持：

资源查询： \> 有哪些 Agent 方向专家？

关系查询： \> 某企业有哪些高校合作关系？

推荐查询： \> 推荐适合合作的专家

------------------------------------------------------------------------

# 5.7 自动发现与维护

系统周期性扫描数据变化：

自动发现： - 新专家 - 新论文 - 新机构动态 - 新合作关系

生成资源更新提醒。

------------------------------------------------------------------------

# 6. 非功能需求

## 性能要求

支持： - 万级节点规模 - 十万级关系规模 - 秒级查询响应

## 可扩展性

支持： - 新实体类型 - 新数据源 - 新 AI 模型

## 数据安全

支持： - 用户权限控制 - 数据访问控制 - 操作日志

------------------------------------------------------------------------

# 7. 系统技术架构

数据源层：

论文、网页、Excel、文档

↓

AI处理层：

LLM、Embedding、实体识别、关系抽取

↓

知识图谱层：

Neo4j

↓

服务层：

FastAPI

↓

前端：

React + 图谱可视化组件

------------------------------------------------------------------------

# 8. 数据存储设计

## 图数据库

Neo4j：

存储： - 节点 - 边 - 关系查询

## 关系数据库

PostgreSQL：

存储： - 用户 - 权限 - 配置 - 日志

------------------------------------------------------------------------

# 9. MVP开发规划

## 第一阶段：基础图谱系统

周期：2-4周

功能： - Excel导入 - 实体管理 - 关系管理 - 图谱展示 - 搜索查询

## 第二阶段：AI增强

周期：4-8周

功能： - PDF解析 - 自动实体抽取 - 自动关系生成 - 自动画像生成

## 第三阶段：智能助手

功能： - AI问答 - 关系推荐 - 自动维护 - 主动提醒

------------------------------------------------------------------------

# 10. 后续扩展

## 合作机会发现

结合： - 技术趋势 - 资源网络 - 外部动态

自动发现合作机会。

## 技术趋势分析

结合： - 论文 - 新闻 - 开源项目

形成技术趋势地图。

------------------------------------------------------------------------

# 11. 项目愿景

构建 AI 驱动的合作资源智能管理平台。

实现：

资源数字化\
↓\
关系网络化\
↓\
知识智能化\
↓\
合作决策辅助

最终成为技术合作经理的智能工作平台。
